apiVersion: v1
kind: Service
metadata:
  name: {{ include "redash5.fullname" . }}
  labels:
    app: {{ include "redash5.name" . }}
    chart: {{ include "redash5.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "redash5.name" . }}
    release: {{ .Release.Name }}
